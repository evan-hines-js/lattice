apiVersion: lattice.dev/v1alpha1
kind: CedarPolicy
metadata:
  name: radarr-security
  namespace: lattice-system
spec:
  description: Allow radarr security overrides for linuxserver.io image
  policies: |
    permit(
      principal == Lattice::Service::"media/radarr",
      action == Lattice::Action::"OverrideSecurity",
      resource == Lattice::SecurityOverride::"capability:SETUID"
    );
    permit(
      principal == Lattice::Service::"media/radarr",
      action == Lattice::Action::"OverrideSecurity",
      resource == Lattice::SecurityOverride::"capability:SETGID"
    );
    permit(
      principal == Lattice::Service::"media/radarr",
      action == Lattice::Action::"OverrideSecurity",
      resource == Lattice::SecurityOverride::"capability:CHOWN"
    );
    permit(
      principal == Lattice::Service::"media/radarr",
      action == Lattice::Action::"OverrideSecurity",
      resource == Lattice::SecurityOverride::"capability:DAC_OVERRIDE"
    );
    permit(
      principal == Lattice::Service::"media/radarr",
      action == Lattice::Action::"OverrideSecurity",
      resource == Lattice::SecurityOverride::"capability:FOWNER"
    );
    permit(
      principal == Lattice::Service::"media/radarr",
      action == Lattice::Action::"OverrideSecurity",
      resource == Lattice::SecurityOverride::"runAsRoot"
    );
    permit(
      principal == Lattice::Service::"media/radarr",
      action == Lattice::Action::"OverrideSecurity",
      resource == Lattice::SecurityOverride::"readWriteRootFilesystem"
    );
    permit(
      principal == Lattice::Service::"media/radarr",
      action == Lattice::Action::"OverrideSecurity",
      resource == Lattice::SecurityOverride::"apparmor:Unconfined"
    );
    permit(
      principal == Lattice::Service::"media/radarr",
      action == Lattice::Action::"OverrideSecurity",
      resource == Lattice::SecurityOverride::"allowedBinary:*"
    );
    permit(
      principal == Lattice::Service::"media/radarr",
      action == Lattice::Action::"AccessExternalEndpoint",
      resource == Lattice::ExternalEndpoint::"api.nzbgeek.info:443"
    );
---
apiVersion: lattice.dev/v1alpha1
kind: LatticeService
metadata:
  name: radarr
  namespace: media
spec:
  workload:
    containers:
      main:
        image: docker.io/linuxserver/radarr:latest
        security:
          runAsUser: 0
          readOnlyRootFilesystem: false
          apparmorProfile: Unconfined
          capabilities: [CHOWN, DAC_OVERRIDE, FOWNER, SETUID, SETGID]
          allowedBinaries: ["*"]
        variables:
          PUID: "1000"
          PGID: "1000"
          TZ: "America/New_York"
        volumes:
          /config:
            source: ${resources.config}
          /downloads:
            source: ${resources.media-storage}
            path: downloads
          /movies:
            source: ${resources.media-storage}
            path: movies
          /tmp: {}
          /run: {}
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 1000m
            memory: 1Gi
        readinessProbe:
          httpGet:
            path: /ping
            port: 7878
    service:
      ports:
        http:
          port: 7878
          protocol: TCP
    resources:
      config:
        type: volume
        params:
          size: 5Gi
      media-storage:
        type: volume
        id: media-storage
      nzbget:
        type: service
        direction: outbound
      prowlarr:
        type: service
        direction: inbound
      nzbgeek:
        type: external-service
        direction: outbound
        params:
          endpoints:
            https: api.nzbgeek.info:443
  replicas: 1
  advertise:
    allowedServices: ["*"]
  ingress:
    gatewayClass: istio
    routes:
      public:
        hosts:
          - radarr.home.arpa
        tls:
          issuerRef:
            name: homelab-selfsigned
