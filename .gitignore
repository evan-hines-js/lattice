/target
**/target/
examples/homelab/route-adapter/Cargo.lock
tarpaulin-report.html
*.profraw
.env
.regcred.json
/test-charts
# Ignore everything under /test-providers except basis — basis is a
# private/internal CAPI provider with no upstream release URL, so its
# rendered components are vendored here for CI.
/test-providers/*
!/test-providers/infrastructure-basis
/endurance-logs
.*.credentials
